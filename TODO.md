# TODO

Переехал из `LeserMashine/todo.txt` — старый Node-бэкенд из репозитория удалён,
его заменил `api/`.

## Сделано

- [x] **Два раздела: мои книги и библиотека.** Оба приходят одним `/getAll`,
      «мои» — фильтр по флагу `mine`, который считает сервер. Вкладки в `Home.jsx`.
- [x] **Сохранение места в книге в БД.** Таблица `reading_progress`,
      `POST /pageWasFlipped` — один upsert вместо перезаписи массива `pages`,
      как было в Mongo.
- [x] **Админ и юзер.** Права проверяет SQL в `deleteOne`: своя книга либо админ.
      В ответе книги есть `canDelete`, и крестик на карточке появляется только там,
      где удаление действительно разрешено.
- [x] **Поле «кем добавлено».** `books.owner_id` → `addedBy` в ответе,
      подпись «добавил {username}» внизу карточки.
- [x] **Удаление аккаунта.** `DELETE /account`: кого удалять, сервер берёт из
      токена, тело запроса не читается. Закладки уходят каскадом,
      загруженные книги остаются в библиотеке с `owner_id = null`.
- [x] **Сборка на прод, докер, тесты на гите.** `.github/workflows/ci.yml`:
      три параллельные джобы (api, frontend, docker). Dockerfile'ы обоих сервисов
      многостадийные — в финальный образ едет только бинарник и `dist`.
      Прод-конфиг nginx лежит в `deploy/nginx.prod.conf`.

## Не делаем

- **Синхронизация книг с Mongo.** Источник правды один — Postgres, старый бэкенд
  удалён, `/synchronizeBooks` из него убран намеренно. Двусторонняя
  синхронизация с базой, которую выключают, — это код с гарантированным сроком
  жизни в пару недель плюс разрешение конфликтов записи. Разовый перенос уже
  есть: `api/migrate-from-mongo.js` (умеет `--dry-run`).
  Если Mongo должна пожить дольше, чем предполагает README, — вывод меняется.

## Дальше, когда понадобится

- Корень репозитория не под git, `frontend/` — вложенный репозиторий со своей
  историей. Пока это так, CI из `.github/` никуда не поедет.
- Пагинация в `/getAll`: сейчас отдаётся вся библиотека одним списком.
  Появится тысяча книг — `limit`/`offset` и отдельный запрос за «моими».
- Отзыв токена: `users.token_version` в claims. Закрывает сразу два случая —
  «выйти на всех устройствах» и живой токен удалённого аккаунта.
