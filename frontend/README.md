# leser-frontend

React + Vite. Описание проекта и запуск — в [README в корне](../README.md),
контракт API — в [api/README.md](../api/README.md).

```sh
npm ci
npm run dev     # :5173, ходит в API через vite-прокси (VITE_API_TARGET)
npm run lint
npm run build
```
