-- Версия разбора. Разбор меняется, когда умнеет парсер (главы, сноски,
-- оглавление), и тогда лежащие в book_docs документы устаревают все разом.
-- Чистить таблицу руками на каждом сервере — это и есть то, что забывают.
-- Вместо этого /read сравнивает версию строки с DOC_VERSION в коде: не та —
-- собирает заново и перезаписывает. Старые строки — версия 0, то есть
-- пересоберутся при первом открытии.
alter table book_docs add column version integer not null default 0;
