-- «Кем добавлено» становится «источником». Для загруженной книги источник —
-- это читатель (owner_id), для найденной снаружи — адрес, откуда её взяли.
-- Одно поле вместо двух понятий: в списке всегда написано, откуда книга.
--
-- owner_id при этом остаётся и продолжает решать права: удалить книгу может
-- тот, кто её принёс, — неважно, из файла или по ссылке.
alter table books add column source_url text;
